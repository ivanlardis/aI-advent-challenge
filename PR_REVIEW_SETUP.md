# Настройка автоматического PR Review

## ✅ Что реализовано

Автоматическая система code review для Pull Request с использованием AI (Grok 4.1 Fast).

### Компоненты:

1. **GitDiffExtractor** - получение измененных файлов через `gh pr diff --name-only` и `git diff --name-status`
2. **ReviewPromptBuilder** - формирование структурированных промптов для LLM
3. **PRAnalysisService** - координация анализа и вызов LLM
4. **GitHubCommentPublisher** - публикация результатов в PR через `gh pr comment`
5. **ReviewCommand** - CLI интерфейс
6. **GitHub Actions Workflow** - автоматизация в CI/CD

### Особенности:

- Анализирует только .kt, .java, .md файлы
- Лимит: максимум 30 файлов, 300 строк на файл в промпте
- Фокус на качество кода (стиль, читаемость, сложность, дубликаты)
- Использует Grok 4.1 Fast через OpenRouter API

## 🚀 Настройка для GitHub Actions

### Шаг 1: Добавить секрет

1. Откройте ваш GitHub репозиторий
2. Перейдите в **Settings → Secrets and variables → Actions**
3. Нажмите **New repository secret**
4. Имя: `OPENROUTER_API_KEY`
5. Значение: ваш API ключ OpenRouter (начинается с `sk-or-v1-...`)
6. Сохраните

### Шаг 2: Проверить workflow

Файл `.github/workflows/pr-review.yml` уже создан и настроен. Он автоматически запускается при:
- Создании нового PR (`opened`)
- Обновлении PR (`synchronize`)
- Переоткрытии PR (`reopened`)

### Шаг 3: Протестировать

1. Создайте тестовый PR в вашем репозитории
2. Workflow запустится автоматически
3. Проверьте вкладку **Actions** для просмотра выполнения
4. Через 2-3 минуты бот опубликует комментарий в PR

### Шаг 4: Если возникли ошибки

Проверьте логи в **Actions** и убедитесь:
- `OPENROUTER_API_KEY` добавлен в secrets
- PR содержит изменения в поддерживаемых файлах (.kt, .java, .md)
- У workflow есть права на `pull-requests: write`

## 🧪 Локальное тестирование

### Требования:

- JDK 17+
- gh CLI установлен: https://cli.github.com/
- Аутентификация через `gh auth login`

### Команды:

```bash
# 1. Собрать проект
./gradlew shadowJar

# 2. Установить переменные окружения
export OPENROUTER_API_KEY="sk-or-v1-YOUR-KEY"
export GITHUB_TOKEN="$(gh auth token)"
export GITHUB_REPOSITORY="owner/repo"

# 3. Запустить ревью для PR #123
java -jar build/libs/project-assistant-1.0.0.jar review-pr --pr-number=123
```

### Пример вывода:

```
🤖 AI Code Review для PR #123

📊 Получение diff...
✅ Анализ завершен (файлов: 5)

📝 Публикация комментария в PR...
✅ Комментарий успешно опубликован!

Просмотреть: gh pr view 123
```

## 📋 Формат комментария

Бот автоматически публикует структурированный комментарий:

```markdown
## 📊 Общая оценка
✅ Хорошо / ⚠️ Требует доработки / ❌ Критические проблемы

## ✅ Что хорошо
- [Позитивные аспекты]

## ⚠️ Замечания
- **file.kt**: [Проблема и рекомендация]

## 💡 Рекомендации
- [Общие советы]

---
⚠️ PR содержит N файлов, проанализированы первые 30
*Проанализировано файлов: 5 | Powered by Grok 4.1 Fast*
```

## ⚙️ Конфигурация

Настройки в `src/main/kotlin/config/Config.kt`:

```kotlin
// Review limits
const val REVIEW_MAX_FILES = 30          // Максимум файлов для анализа
const val REVIEW_MAX_LINES_PER_FILE = 500 // Лимит строк в промпте

// Поддерживаемые расширения
val SUPPORTED_EXTENSIONS = setOf("kt", "java", "md")
```

## 🔧 Устранение проблем

### Workflow не запускается

- Проверьте, что файл `.github/workflows/pr-review.yml` закоммичен в main/master
- Убедитесь, что Actions включены в Settings → Actions

### Ошибка "OPENROUTER_API_KEY not found"

- Добавьте секрет в Settings → Secrets and variables → Actions
- Имя должно быть точно `OPENROUTER_API_KEY`

### Ошибка "gh CLI не найден"

- Локально: установите gh CLI https://cli.github.com/
- В Actions: проблемы быть не должно (gh предустановлен)

### Комментарий не публикуется

- Проверьте права workflow: `pull-requests: write`
- Убедитесь, что `GITHUB_TOKEN` передается в workflow

### "Нет файлов для анализа"

- PR содержит только binary файлы или неподдерживаемые расширения
- Добавьте нужные расширения в `Config.SUPPORTED_EXTENSIONS`

## 📚 Дополнительно

### Отключить для конкретного PR

Добавьте в описание PR:
```
[skip ci]
```

### Изменить модель

В `Config.kt`:
```kotlin
const val OPENROUTER_MODEL = "anthropic/claude-3-5-sonnet" // Вместо Grok
```

### Добавить фильтры

Отредактируйте `.github/workflows/pr-review.yml`:
```yaml
on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main  # Только для PR в main
```

## 🎯 Следующие улучшения (опционально)

1. **RAG интеграция** - индексировать измененные файлы для поиска похожих паттернов
2. **Инлайн комментарии** - комментарии к конкретным строкам кода
3. **Метки PR** - автоматически добавлять labels (needs-review, bug, enhancement)
4. **Кэширование** - кэшировать JAR между запусками для ускорения
5. **Метрики** - собирать статистику по ревью

## 📞 Поддержка

При проблемах проверьте:
1. Логи GitHub Actions
2. Права workflow
3. Наличие секрета OPENROUTER_API_KEY
4. Версию gh CLI (в Actions всегда актуальная)
