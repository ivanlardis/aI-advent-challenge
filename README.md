# Project Assistant

RAG CLI-ассистент для разработчика на Kotlin.

## Описание

Standalone CLI-приложение, которое индексирует документацию проекта (Java, Kotlin, Markdown файлы) и отвечает на вопросы с использованием RAG и OpenRouter API.

## Возможности

- 🔍 Индексация исходного кода проекта (.kt, .java, .md)
- 🤖 Ответы на вопросы с использованием RAG + LLM
- 🔧 Интеграция с Git (ветка, последний коммит, статус)
- 💬 Интерактивный CLI режим
- 📦 Упаковка в single JAR файл

## Установка

### Требования

- JDK 17+
- Gradle 8.x

### Сборка

```bash
./gradlew clean build shadowJar
```

Результат: `build/libs/project-assistant-1.0.0.jar`

## Использование

### Запуск

```bash
java -jar build/libs/project-assistant-1.0.0.jar --home /path/to/your/project
```

### Конфигурация

Перед запуском укажите API ключ OpenRouter в файле:
`src/main/kotlin/config/Config.kt`

```kotlin
const val OPENROUTER_API_KEY = "sk-or-v1-YOUR-KEY-HERE"
const val OPENROUTER_MODEL = "anthropic/claude-3.5-sonnet"
```

### Команды

После запуска доступны следующие команды:

- `/index` - Индексировать проект
- `/help` - Показать справку
- `/git` - Информация о git-репозитории
- `/stats` - Статистика индекса
- `/exit` - Выход

### Примеры

```bash
> /index
Начинаю индексацию проекта...
Найдено файлов: 42
Вычисление статистики...
Индексация документов...
Индексация завершена!

=== Результат индексации ===
Всего файлов: 42
  Kotlin:   30
  Java:     5
  Markdown: 7
Всего токенов: 15430

> Как работает аутентификация?
Поиск релевантных документов...
Найдено документов: 5
  1. src/auth/AuthService.kt (score: 0.87)
  2. src/auth/JwtProvider.kt (score: 0.72)
  ...

Отправка запроса к LLM...

=== Ответ ===
Аутентификация в проекте реализована через JWT токены...
```

## Архитектура

```
src/main/kotlin/
├── Main.kt                   # Точка входа
├── config/
│   └── Config.kt            # Конфигурация
├── cli/
│   └── AssistantCommand.kt  # CLI интерфейс
├── rag/
│   ├── TfIdfVectorizer.kt   # Векторизация
│   ├── InMemoryVectorStore.kt
│   ├── RagService.kt
│   └── DocumentIndexer.kt
├── llm/
│   ├── OpenRouterClient.kt  # LLM клиент
│   └── PromptBuilder.kt
├── mcp/
│   ├── GitTools.kt          # Git операции
│   └── McpServer.kt
└── parser/
    ├── FileParser.kt
    ├── KotlinParser.kt
    ├── JavaParser.kt
    └── MarkdownParser.kt
```

## Технологии

- **Kotlin 1.9** - язык программирования
- **Gradle 8** - сборка проекта
- **Shadow Plugin** - упаковка в fat JAR
- **Clikt** - CLI framework
- **Ktor Client** - HTTP клиент для OpenRouter
- **JGit** - Git интеграция
- **TF-IDF** - векторизация текста

## Разработка

### Запуск в dev режиме

```bash
./gradlew run --args="--home /path/to/project"
```

### Тесты

```bash
./gradlew test
```

## Лицензия

MIT
