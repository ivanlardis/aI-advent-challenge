## AI Advent Challenge — Тестирование System Prompts

Чат-бот с возможностью смены роли агента (system prompt) в реальном времени. История диалога сохраняется при переключении между ролями.

### Переменные окружения
- `OPENROUTER_API_KEY` — API ключ с https://openrouter.ai
- `OPENROUTER_MODEL` — модель (по умолчанию `tngtech/deepseek-r1t2-chimera:free`)

### Запуск

**Локально:**
```bash
pip install -r requirements.txt
OPENROUTER_API_KEY=xxx chainlit run app/chainlit_app.py
```

**Docker:**
```bash
docker build -t lardis .
docker run -d -p 8000:8000 -e OPENROUTER_API_KEY=xxx lardis
```

**Production (с рекомендуемой моделью):**
```bash
docker run -d --name lardis -p 38542:8000 --restart=unless-stopped \
  -e OPENROUTER_API_KEY=xxx \
  -e OPENROUTER_MODEL=x-ai/grok-4.1-fast:free \
  ghcr.io/ivanlardis/lardis:0.0.1
```

### Как это работает

1. При запуске доступны 4 предустановленные роли:
   - **nutritionist** — нутрициолог для расчёта БЖУ
   - **strict_teacher** — строгий преподаватель Python
   - **friendly_mentor** — дружелюбный наставник
   - **code_reviewer** — критичный код-ревьюер

2. Смена роли через UI (⚙️ в верхнем правом углу)
3. История сообщений сохраняется при переключении ролей
4. Каждая роль использует свой system prompt с уникальным стилем общения
5. Все ответы возвращаются в формате JSON для единообразной обработки

### План тестирования (из задания)

1. Выбрать тему диалога (например: "объяснение рекурсии в Python")
2. Начать диалог с роли `strict_teacher`
3. Провести 5-10 сообщений, зафиксировать:
   - Тон (формальный/неформальный)
   - Структура (краткие/развёрнутые ответы)
   - Глубина объяснений
   - Наличие вопросов от агента
4. Переключиться на `friendly_mentor` (через ⚙️)
5. Продолжить диалог на ту же тему
6. Сравнить различия в стиле, примерах, способе подачи информации
7. Повторить с другими ролями для разных сценариев

### Пример диалога (strict_teacher → friendly_mentor)

```
[Роль: strict_teacher]
👤 Объясни что такое рекурсия

🤖 Рекурсия — это когда функция вызывает сама себя.
    Каковы два обязательных условия для корректной рекурсии?

👤 База и рекурсивный случай?

🤖 Верно. Теперь задача: напишите функцию factorial(n) рекурсивно.

[Пользователь переключается на friendly_mentor через ⚙️]

✅ System prompt изменён на: Дружелюбный наставник
   История диалога сохранена. Продолжайте общение!

👤 А можешь показать пример с факториалом?

🤖 Конечно! Представь, что тебе нужно посчитать 5! = 5×4×3×2×1.
    Рекурсия работает как матрёшка: каждая функция открывает следующую,
    пока не дойдёт до самой маленькой (база).

    Вот код:
    ```python
    def factorial(n):
        if n <= 1:  # база — открыли последнюю матрёшку
            return 1
        return n * factorial(n - 1)  # рекурсия — открываем следующую
    ```

    Попробуй мысленно "распаковать" factorial(3), будет интересно! 😊
```

### Различия в стилях (пример анализа)

| Аспект | strict_teacher | friendly_mentor |
|--------|----------------|-----------------|
| Тон | Формальный, строгий | Неформальный, дружеский |
| Ответы | Краткие определения | Развёрнутые объяснения с аналогиями |
| Примеры | Минимум примеров | Много примеров и метафор ("как матрёшка") |
| Вопросы | Проверочные задачи | Поощряющие эксперименты |
| Эмодзи | Нет | Используются для поддержки |
