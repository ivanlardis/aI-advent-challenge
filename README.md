# AI Advent Chat

Простое Chainlit приложение для диалогов с LLM через OpenRouter с сохранением истории в PostgreSQL.

## Возможности

- 💬 Чат с AI через OpenRouter
- 📝 Сохранение истории диалогов в PostgreSQL
- 🔄 Восстановление истории при обновлении страницы
- 🔐 Простая авторизация (admin/1234)
- 🐳 Запуск через Docker Compose
- ✉️ Email-рассылка сводки напоминаний вручную или по расписанию

## Email-рассылка сводки

Инструмент `send_summary_email` доступен в MCP и может быть вызван из чата. Он собирает все напоминания и отправляет их на указанный email.

Настройте почту через Mailtrap Send API по токену (пример в `.env`):

```
MAILTRAP_API_TOKEN=<mailtrap_api_token>
MAILTRAP_SENDER_EMAIL=notify@example.com
MAILTRAP_SENDER_NAME=Reminders Bot
SUMMARY_EMAIL_TO=me@example.com

# Опционально: автоотправка раз в сутки
SUMMARY_EMAIL_AUTO=true
SUMMARY_EMAIL_TIME=09:00      # HH:MM (локальное время контейнера)
SUMMARY_EMAIL_CHECK_INTERVAL=60
```

Принудительная отправка: вызовите MCP-инструмент `send_summary_email` и передайте `email_to`, если нужно отправить на другой адрес. Автоотправка включается флагом `SUMMARY_EMAIL_AUTO=true`; если `SUMMARY_EMAIL_TO` не задан, автоотправка пропускается.
