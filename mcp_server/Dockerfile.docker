FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY docker_server.py .

EXPOSE 1223
CMD ["uvicorn", "docker_server:app", "--host", "0.0.0.0", "--port", "1223"]
