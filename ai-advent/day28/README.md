# 🔍 Локальный аналитик данных

Анализ CSV, JSON и логов с помощью локальной LLM-модели через Ollama.

## 📋 Описание

Это приложение позволяет анализировать структурированные данные (CSV, JSON, текстовые логи) с помощью локально запущенной языковой модели. Все данные обрабатываются на вашей машине и никуда не отправляются.

## ✨ Возможности

- 📊 Поддержка CSV, JSON и LOG файлов
- 🤖 Анализ с помощью LLM (qwen2.5-coder:7b через Ollama)
- 💬 Интерактивный чат-интерфейс
- 🔒 Полностью локальная обработка данных
- 📝 Адаптивные подсказки вопросов под тип данных
- ⚡ Автоматический запуск Ollama при необходимости

## 🛠️ Требования

- Python 3.9+
- [Ollama](https://ollama.ai/) (установлен и настроен)

## 📦 Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что Ollama установлена и скачана модель:
```bash
ollama pull qwen2.5:0.5b
```

Или любую другую модель, например: `llama3.2`, `mistral`, `gemma3`.

## 🚀 Запуск

**Способ 1: Через скрипт (рекомендуется)**
```bash
./start.sh
```

**Способ 2: Прямой запуск**
```bash
python3 -m chainlit run app.py
```

Приложение откроется в браузере на `http://localhost:8000`.

## 📖 Использование

1. **Загрузите файл** через интерфейс (кнопка загрузки или drag-and-drop)
2. **Дождитесь обработки** — вы увидите информацию о файле и примеры вопросов
3. **Задавайте вопросы** в чате о ваших данных
4. **Получайте ответы** на основе анализа LLM

## 💡 Примеры вопросов

### Для CSV файлов:
- "Сколько всего записей в файле?"
- "Какие уникальные значения в каждой колонке?"
- "Покажи статистику по числовым полям"
- "Есть ли дубликаты?"

### Для JSON файлов:
- "Какая структура данных?"
- "Сколько объектов на верхнем уровне?"
- "Какие поля присутствуют во всех записях?"
- "Покажи сводку по данным"

### Для LOG файлов:
- "Какая ошибка встречается чаще всего?"
- "Сколько записей уровня ERROR/WARN/INFO?"
- "В какое время произошло больше всего ошибок?"
- "Какие основные события в логе?"

## 🧪 Тестовые данные

В директории `test_data/` находятся примеры файлов для тестирования:
- `sample.csv` — CSV с пользователями
- `sample.json` — JSON с действиями пользователей
- `sample.log` — Лог-файл приложения

## ⚙️ Ограничения

- **Максимальный размер файла**: 10MB
- **Поддерживаемые форматы**: .csv, .json, .log, .txt
- **Требуется**: Локально запущенная Ollama
- **Точность**: LLM может ошибаться в числовых подсчётах

## 🔧 Устранение неполадок

### Ollama не запускается автоматически
Запустите Ollama вручную:
```bash
ollama serve
```

### Модель не найдена
Скачайте модель (или измените `OLLAMA_MODEL` в `app.py` на доступную):
```bash
ollama pull qwen2.5:0.5b
```

### Ошибка при парсинге файла
- Убедитесь, что файл не пустой
- Проверьте корректность формата (валидный JSON, правильный CSV)
- Размер файла не должен превышать 10MB

## 📝 Структура проекта

```
day28/
├── app.py              # Основное приложение
├── requirements.txt    # Зависимости Python
├── README.md          # Документация
├── SPEC.md            # Техническая спецификация
└── test_data/         # Тестовые данные
    ├── sample.csv
    ├── sample.json
    └── sample.log
```

## 🎯 AI Advent Challenge

Это решение для **Day 29** челленджа AI Advent 2024.

**Задача**: Создать локальный аналитик данных с использованием LLM для анализа CSV, JSON и логов без отправки данных в облако.

## 📜 Лицензия

MIT

## 🤝 Вклад

Это учебный проект в рамках AI Advent Challenge. Форки и улучшения приветствуются!
