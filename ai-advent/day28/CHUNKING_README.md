# Chunked-обработка больших файлов

## ✨ Что реализовано

Система автоматической обработки больших файлов через разбиение на чанки с агрегацией результатов от LLM.

### Основные возможности

✅ **Автоматическое определение стратегии**
- Файлы ≤1000 строк → обычная обработка (один запрос)
- Файлы >1000 строк → chunked-обработка

✅ **Адаптивный размер чанков**
- Базовый: 500 строк
- Для больших файлов (>50K строк): 1000 строк
- Максимум: 100 чанков

✅ **Поддержка всех форматов**
- **LOG/TXT**: Простое разбиение по строкам
- **CSV**: Добавление header row в каждый чанк
- **JSON**: Разбиение по top-level элементам/объектам

✅ **Умная агрегация**
- Простая агрегация для метрик (сумма в коде)
- Двухуровневая агрегация для >10 чанков (Map-Reduce)
- LLM-агрегация для сложных запросов

✅ **UX**
- Прогресс-бар с процентами обработки
- Информация о режиме обработки при загрузке файла
- Автоматический сброс истории при смене файла

✅ **Надёжность**
- Retry до 2 раз при ошибках парсинга JSON
- Few-shot примеры для маленьких моделей
- Fallback на извлечение чисел из текста

## 🚀 Как использовать

### 1. Запуск приложения

```bash
cd ai-advent/day28
python3 -m chainlit run app.py
```

Откройте http://localhost:8000

### 2. Загрузка файла

Перетащите файл в интерфейс или используйте кнопку загрузки.

**Пример с sample.log (2MB, ~40K строк):**

```
📄 Загружен новый файл. История чата сброшена.

**LOG файл загружен!**

- Строк: 40000

💡 **Режим обработки:** Chunked (файл большой)
- Будет обработано частями: ~80 чанков по 500 строк
```

### 3. Задайте вопрос

**Примеры вопросов:**

Простые метрики (быстрая обработка):
- "Сколько ошибок ERROR в логе?"
- "Сколько записей с user_id=123?"
- "Какое общее количество записей?"

Сложные запросы (медленнее, но точнее):
- "Сравни количество ошибок в первой и последней половине лога"
- "Какие самые частые типы ошибок?"
- "Покажи динамику ошибок по времени"

### 4. Процесс обработки

```
📊 Файл большой (40000 строк). Обработка по частям: 80 чанков по 500 строк.

🔄 Обработано 15/80 (19%)
🔄 Обработано 30/80 (38%)
...
🔄 Обработано 80/80 (100%)

✨ Формирую итоговый ответ...

[Финальный ответ]
```

## 🔧 Технические детали

### Структура модулей

```
app.py                      # Основное приложение Chainlit
chunking.py                 # Модуль chunked-обработки
test_chunking.py           # Юнит-тесты
CHUNKED_PROCESSING_SPEC.md # Полная спецификация
```

### Ключевые функции

**chunking.py:**
- `should_use_chunking()` - Определение стратегии
- `chunk_data()` - Разбиение на чанки
- `build_chunk_prompt()` - Создание промпта
- `aggregate_simple()` - Простая агрегация
- `build_aggregation_prompt()` - LLM-агрегация

**app.py:**
- `process_chunk()` - Обработка одного чанка с retry
- `process_chunked()` - Полная chunked-обработка
- `main()` - Выбор стратегии обработки

### Формат ответа чанка

```json
{
  "count": 12,
  "items": ["item1", "item2", ...],
  "summary": "Краткое описание результата"
}
```

## 📊 Производительность

### Тестирование на sample.log

**Файл:** 2MB, ~40,000 строк

**Вопрос:** "Сколько ошибок ERROR?"

**Процесс:**
1. Разбиение на 80 чанков по 500 строк
2. Обработка каждого чанка: ~4-5 сек
3. Агрегация: ~5 сек
4. **Общее время:** ~6-7 минут

**Оптимизации для ускорения:**
- Уменьшить размер sample.log до 5K строк → 10 чанков → ~1 минута
- Использовать более быструю модель (qwen2.5:1.5b вместо 0.5b)
- Включить параллельную обработку (будущая оптимизация)

## 🎯 Примеры использования

### Пример 1: Подсчёт ошибок

**Вопрос:** "Сколько ошибок ERROR в логе?"

**Результат:**
```
Найдено 247 записей уровня ERROR в логе.
```

**Как работает:**
1. Каждый чанк подсчитывает ERROR в своей части
2. Код суммирует: `total = sum(counts)`
3. LLM форматирует ответ

### Пример 2: Анализ CSV

**Файл:** Sales data, 50K строк

**Вопрос:** "Какая средняя сумма продажи?"

**Результат:**
```
На основе анализа 50,000 записей:
Средняя сумма продажи: $145.67
Общая сумма: $7,283,500
```

### Пример 3: Временной анализ

**Вопрос:** "Сравни ошибки в первой и второй половине дня"

**Результат:**
```
Первая половина (00:00-12:00): 45 ошибок
Вторая половина (12:00-24:00): 78 ошибок
Рост на 73% во второй половине дня
```

## ⚙️ Конфигурация

В `chunking.py`:

```python
BASE_CHUNK_SIZE = 500           # Базовый размер чанка
LARGE_FILE_THRESHOLD = 50_000   # Порог для увеличения чанка
LARGE_CHUNK_SIZE = 1000         # Размер для больших файлов
MAX_CHUNKS = 100                # Максимум чанков
CHUNKING_THRESHOLD = 1000       # Порог активации chunking
```

## 🐛 Известные ограничения

1. **Время обработки:** Большие файлы могут обрабатываться до 10 минут
2. **Межчанковый контекст:** Вопросы требующие связи между далёкими частями файла могут быть менее точными
3. **JSON в памяти:** JSON файлы загружаются полностью (ограничение текущей реализации)
4. **Маленькая модель:** qwen2.5:0.5b может нестабильно возвращать JSON (решается retry)

## 🚧 Будущие улучшения

- [ ] Параллельная обработка чанков (3-4 одновременно)
- [ ] Streaming промежуточных результатов
- [ ] JSON streaming parser (ijson) для экономии памяти
- [ ] Кэширование результатов для повторных вопросов
- [ ] Поддержка мульти-файлового анализа

## 📝 Тестирование

Запуск юнит-тестов:

```bash
python3 test_chunking.py
```

Проверяет:
- Определение стратегии
- Вычисление размера чанков
- Разбиение LOG/CSV/JSON
- Построение промптов
- Агрегацию результатов

## 📚 Документация

Полная спецификация: `CHUNKED_PROCESSING_SPEC.md`

Включает:
- Детальную архитектуру
- План реализации по фазам
- Примеры кода
- Риски и миtigations
- Метрики и мониторинг
