# Project Starter CLI Configuration
# Копируйте этот файл в .env и заполните своими данными

# GitHub Token (ВАЖНО: нужны scopes: repo И workflow!)
# Создать токен: https://github.com/settings/tokens
# Scopes:
#   - repo (full control of private repositories)
#   - workflow (для GitHub Actions файлов)
GITHUB_TOKEN=your_github_token_here

# VPS Configuration
VPS_HOST=213.171.25.139
VPS_USER=ivanlardis

# VPS Password (если включена парольная аутентификация)
# VPS_PASSWORD=your_password_here

# Или используйте SSH-ключи вместо пароля
# Добавьте свой публичный ключ на VPS:
# cat ~/.ssh/id_ed25519.pub | ssh user@vps 'mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys'
