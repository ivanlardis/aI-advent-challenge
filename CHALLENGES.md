# AI Advent Challenges

## Challenge 19 - RAG чат-бот с историей и источниками

**Дата:** 2025-12-25

### Задание
Реализовать простой чат-бот, который:
- Хранит историю диалога
- При каждом новом вопросе ищет контекст в базе документов (через RAG)
- Возвращает ответ с учётом найденной информации
- **Обязательно выводит источники**, откуда был взят ответ

### Реализация

**Подход:** Улучшение существующего Chainlit приложения минимальными изменениями.

**Изменения в `app/chat/chainlit_app.py`:**

1. **Убрана условность RAG** - функция `should_use_rag()` теперь всегда возвращает `True`
2. **Добавлена функция `format_sources()`** - форматирует источники в красивый markdown
3. **Добавлена переменная `rag_sources`** - хранит результаты поиска для вывода
4. **Сохранение `filtered_results` в `rag_sources`** - передача данных для источников
5. **Улучшен `system_prompt`** - модель знает, что источники добавятся автоматически
6. **Финальное сообщение с источниками** - автоматическое добавление секции "📚 Источники"
7. **История включает источники** - `final_message` сохраняется в истории

### Результат

**Что работает:**
- ✅ История диалога хранится в PostgreSQL
- ✅ RAG поиск при каждом вопросе (условность убрана)
- ✅ Ответы с учётом найденной информации из базы
- ✅ **Автоматический вывод источников** с релевантностью в каждом ответе
- ✅ Совместимость с MCP tools, кнопками управления RAG и фильтром релевантности

**Формат источников:**
```markdown
---
📚 Источники:
1. Москва (релевантность: 0.95) — `rag_example_cities_ru.txt`
2. Санкт-Петербург (релевантность: 0.82) — `rag_example_cities_ru.txt`
```

### Технические детали

- **Файл:** `app/chat/chainlit_app.py` (7 точечных правок)
- **RAG система:** CityRAG с ChromaDB + Sentence-Transformers
- **Эмбеддинги:** `paraphrase-multilingual-MiniLM-L12-v2`
- **Фильтрация:** По порогу релевантности (default 0.7)
- **Веб-интерфейс:** Chainlit на порту 8000
