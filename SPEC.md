# Спецификация: Деплой Chainlit + Ollama на удалённый сервер

## Задача челленджа
Интегрировать модель для использования на удалённом сервисе и сделать свой сервис, который работает на локальной модели. Приложение отвечает на запросы пользователя в режиме чата.

**Результат:** удалённая машина, использующая локальную модель.

---

## Архитектура решения

### Тип деплоя
- **All-in-One Docker контейнер** на сервере
- Ollama + Chainlit + Python код в одном контейнере
- Модель вшивается в Docker образ на этапе build

### Компоненты
1. **Ollama** — сервер для инференса моделей
2. **Chainlit** — веб интерфейс чата
3. **gemma2:2b** — языковая модель (2B параметров)
4. **Python код** — существующее приложение (OllamaClient, SessionHistory, Chainlit app)

---

## Требования к серверу

### Минимальные характеристики
- **CPU:** 1-2 ядра
- **RAM:** 4GB (модель займёт ~2GB)
- **Disk:** 10GB+ (Docker образ + модель + логи)
- **OS:** Любая Linux с поддержкой Docker

### Доступ
- **SSH:** парольная аутентификация
- **Network:** публичный IP, доступ по HTTP
- **Port:** 8000 (Chainlit default)

---

## Конфигурация приложения

### Модель
- **Модель:** `gemma2:2b`
- **Temperature:** 0.0-0.3 (низкая, детерминированные ответы)
- **Max tokens:** 1024
- **System prompt:** не используется

### Пользовательский интерфейс
- **Port:** 8000
- **Protocol:** HTTP (без SSL/TLS)
- **Access:** публичный, без авторизации
- **URL:** `http://<SERVER_IP>:8000`

### Производительность
- **Concurrent users:** 1-2
- **Timeout:** 120 секунд
- **Logs:** минимальные (stdout/stderr)

---

## Docker конфигурация

### Base image
- **Базовый образ:** `python:3.12`
- **Установка Ollama:** официальный install script
- **Пользователь:** root

### Стратегия модели
- **Download:** во время `docker build` (RUN ollama pull gemma2:2b)
- **Storage:** внутри Docker образа
- **Persistence:** не нужна (stateless)

### Autostart
- **Restart policy:** да (автозапуск при загрузке сервера)
- **Метод:** `docker run --restart=always`

---

## Деплой

### Метод деплоя
- **Инструмент:** Docker CLI
- **Обновления:** пересборка образа

### Порядок действий на сервере

1. **Установка Docker**
   ```bash
   curl -fsSL https://get.docker.com | sh
   systemctl start docker
   systemctl enable docker
   ```

2. **Сборка образа**
   ```bash
   docker build -t chainlit-ollama .
   ```

3. **Запуск контейнера**
   ```bash
   docker run -d \
     --name chainlit-ollama \
     --restart=always \
     -p 8000:8000 \
     chainlit-ollama
   ```

4. **Проверка работы**
   - Открыть `http://<SERVER_IP>:8000`
   - Должен появиться Chainlit интерфейс

### Обновление
```bash
docker stop chainlit-ollama
docker rm chainlit-ollama
docker build -t chainlit-ollama .
docker run -d --name chainlit-ollama --restart=always -p 8000:8000 chainlit-ollama
```

---

## Код приложения

### Файлы проекта
```
.
├── Dockerfile
├── requirements.txt
├── app/
│   ├── __init__.py
│   ├── chainlit_app.py
│   ├── ollama_client.py
│   └── history.py
└── chainlit.md
```

### Изменения в коде

**app/chainlit_app.py:8**
```python
OLLAMA_HOST = os.getenv("OLLAMA_HOST", "http://localhost:11434")
```
- ✅ Оставить как есть (Ollama в том же контейнере)

**app/ollama_client.py**
- ✅ Без изменений (текущий код достаточен)

**app/history.py**
- ✅ Без изменений (простая менеджер сессии)

**requirements.txt**
```
chainlit==2.3.0
requests==2.32.3
python-dotenv==1.0.1
```
- ✅ Без изменений

---

## Dockerfile

```dockerfile
FROM python:3.12

# Установка зависимостей для Ollama
RUN apt-get update && apt-get install -y \
    curl \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# Установка Ollama
RUN curl -fsSL https://ollama.com/install.sh | sh

# Копирование Python кода
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app /app/app
COPY chainlit.md .

# Скачивание модели gemma2:2b во время сборки
RUN ollama serve & sleep 5 && ollama pull gemma2:2b && pkill ollama

# Экспозиция порта
EXPOSE 8000

# Запуск Ollama в фоне и Chainlit на переднем плане
CMD sh -c 'ollama serve > /dev/null 2>&1 & chainlit run app/chainlit_app.py -h 0.0.0.0 -p 8000'
```

---

## Риски и компромиссы

### Безопасность
- ⚠️ **Публичный доступ без авторизации** — любой человек может использовать сервис
- ⚠️ **HTTP вместо HTTPS** — трафик не шифруется
- ⚠️ **Root user в контейнере** — потенциальный вектор атаки

**Mitigation:** принять для челленджа, менять не требуется

### Производительность
- ⚠️ **1-2 CPU ядра** — медленная генерация токенов (~2-5 токенов/сек)
- ⚠️ **4GB RAM** — мало запаса для других процессов
- ⚠️ **Без очередей** — блокировка при параллельных запросах

**Mitigation:** ограничение 1-2 пользователей, timeout 120 сек

### Надёжность
- ✅ **Autorestart** — контейнер перезапустится при падении
- ⚠️ **No healthcheck** — сложно определить жив ли сервис
- ⚠️ **No backup** — история чатов теряется при restart

**Mitigation:** достаточно для челленджа

### Масштабируемость
- ❌ **Single container** — невозможно горизонтально масштабировать
- ❌ **No load balancer** — одна точка отказа
- ❌ **Stateful** — модель внутри образа, тяжело обновлять

**Mitigation:** не требуется для челленджа

---

## Альтернативные подходы (рассмотренные и отклонённые)

### 1. Ollama отдельный контейнер + docker-compose
**Почему отклонено:** сложнее, чем нужно для челленджа

### 2. Phi-3 Mini вместо gemma2:2b
**Почему отклонено:** выбрали gemma2:2b

### 3. SSL/TLS с Let's Encrypt
**Почему отклонено:** нужен домен, избыточно для челленджа

### 4. Облачный LLM API вместо локальной модели
**Почему отклонено:** противоречит условию челленджа

---

## Проверка работоспособности

### Manual testing checklist

1. ✅ Docker контейнер запущен (`docker ps`)
2. ✅ Ollama отвечает (`curl http://localhost:11434/api/tags`)
3. ✅ Chainlit доступен (`http://<IP>:8000`)
4. ✅ Модель gemma2:2b загружена
5. ✅ Выбор модели в UI работает
6. ✅ Отправка сообщения генерирует ответ
7. ✅ История сообщений сохраняется в сессии
8. ✅ Метрики отображаются (время, количество символов)
9. ✅ Ошибка при недоступности Ollama показывается в UI

---

## Документация

### Минимальная документация

**README.md на сервере:**
```markdown
# Chainlit + Ollama Chat

## Запуск
docker build -t chainlit-ollama .
docker run -d --name chainlit-ollama --restart=always -p 8000:8000 chainlit-ollama

## Доступ
http://<SERVER_IP>:8000

## Остановка
docker stop chainlit-ollama

## Логи
docker logs chainlit-ollama
```

---

## Trade-offs (компромиссы)

| Аспект | Выбор | Почему |
|--------|-------|---------|
| **Деплой** | All-in-One контейнер | Проще, чем docker-compose или k8s |
| **Модель** | gemma2:2b | Баланс размера и качества для 4GB RAM |
| **SSL** | HTTP только | Нет домена, избыточно для челленджа |
| **Auth** | Public access | Не нужна для 1-2 пользователей |
| **Monitoring** | Минимальный | Достаточно docker logs |
| **Update strategy** | Rebuild образа | Проще, чем volume mount |
| **Persistence** | Stateless | История не нужна для челленджа |

---

## Временные показатели

### Сборка образа
- Скачивание base image: ~1-2 мин
- Установка Ollama: ~1 мин
- Скачивание модели gemma2:2b: ~2-5 мин
- Установка Python зависимостей: ~1 мин
- **Итого:** ~5-10 минут

### Запуск контейнера
- Start Ollama: ~5 сек
- Chainlit startup: ~2-3 сек
- **Итого:** ~10 секунд

### Генерация ответа (1-2 CPU ядра)
- ~2-5 токенов/сек
- Ответ 100 токенов: ~20-50 секунд
- Максимальный timeout: 120 секунд

---

## Условия выполнения челленджа

✅ **Выполнено:**
- [x] Локальная модель на удалённом сервере
- [x] Чат интерфейс для взаимодействия
- [x] Ответы на запросы пользователя
- [x] Docker контейнер для простоты деплоя

🎯 **Итог:** сервер с IP <SERVER_IP>:8000提供服务 Chainlit чата с локальной моделью gemma2:2b
