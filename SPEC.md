# Спецификация: Project Starter CLI

## Назначение

CLI-инструмент для быстрого развёртывания Kotlin pet-проектов/MVP с автоматической настройкой CI/CD и деплоем на VPS.

**Бизнес-задача**: Прототипирование идей без ручной настройки инфраструктуры — от идеи до работающего деплоя за минуты.

---

## Архитектура

### CLI (Тонкий клиент-оркестратор)

```
Project Starter CLI
├── Шаблоны (встроенные ресурсы)
├── GitHub API клиент
├── VPS SSH клиент
└── Интерактивный UI (вопрос-ответ)
```

**Пайплайн**:
1. Интерактивные вопросы пользователю
2. Валидация всех параметров (GitHub token, VPS доступ)
3. Создание GitHub репозитория
4. Генерация проекта из шаблона
5. Настройка GitHub Secrets
6. Настройка VPS (Docker + SSH ключи)
7. Push в GitHub
8. GitHub Actions автоматически деплоит на VPS

### Шаблон проекта

**Стек**: Ktor + Exposed + PostgreSQL + Nginx
- **Backend**: Ktor Server, Kotlinx-html, Exposed ORM
- **Database**: PostgreSQL (в Docker)
- **Proxy**: Nginx reverse proxy
- **Build**: Gradle (Kotlin DSL), single module
- **Deploy**: Docker Compose (multi-service: app + db + nginx)

**Структура**:
```
project-template/
├── src/main/kotlin/          # Ktor приложение
├── src/main/resources/       # Конфигурация
├── Dockerfile                # Сборка app контейнера
├── docker-compose.yml        # App + DB + Nginx
├── build.gradle.kts          # Gradle конфиг
└── .github/workflows/deploy.yml  # CI/CD
```

### GitHub Actions Workflow

**Триггер**: Любой push в любую ветку → деплой на VPS

**Этапы**:
1. Build: Сборка Docker image
2. Deploy: SSH подключение к VPS → `docker compose up -d`

**Секреты** (создаёт CLI автоматически):
- `VPS_HOST` - хост VPS
- `VPS_USER` - пользователь SSH
- `SSH_PRIVATE_KEY` - приватный SSH ключ для подключения

---

## UX CLI

### Интерактивный режим

Пример диалога:
```
$ project-starter create

? Название проекта: My Awesome App
? Описание (для GitHub): My first Kotlin MVP
? VPS хост: 192.168.1.100
? VPS пользователь: root
? GitHub token: sk-...

✓ Валидация пройдена
✓ GitHub репозиторий создан: github.com/username/my-awesome-app
✓ Проект сгенерирован
✓ GitHub Secrets настроены
✓ VPS настроен (Docker установлен, SSH ключи добавлены)
✓ Код отправлен в GitHub

🚀 Готово! Ваш проект будет доступен на http://your-vps-ip через 2-3 минуты
```

### Команды

- `create` — создание нового проекта
- `--help` — справка

**Нет флагов**: всё через интерактивные вопросы.

---

## Технические детали

### CLI фреймворк

**Kotlin Clikt** (легковесный, Kotlin-friendly)

### Авторизация в GitHub

**GitHub Personal Access Token (classic)**
- CLI спрашивает токен при запуске
- Сохраняет в `~/.config/project-starter/token` (без шифрования)
- Scope: `repo` (full control)

### Хранение шаблонов

**Встроенные ресурсы** (`resources/templates/`)
- Template files embedded in JAR
- Кастомизация через placeholder-замены:
  - `{{PROJECT_NAME}}` → имя проекта
  - `{{PACKAGE_NAME}}` → Kotlin package
  - `{{DESCRIPTION}}` → описание

### Настройка VPS

**Гибридный подход**:
1. Проверка: `docker --version` через SSH
2. Если не установлен → авто-установка:
   ```bash
   curl -fsSL https://get.docker.com | sh
   systemctl enable docker
   systemctl start docker
   ```
3. Генерация SSH ключей:
   - Приватный → в GitHub Secrets
   - Публичный → `~/.ssh/authorized_keys` на VPS
4. Установка Docker Compose (если нужен)

### Docker Compose структура

**3 сервиса**:
```yaml
services:
  app:      # Ktor приложение
  db:       # PostgreSQL
  nginx:    # Reverse proxy
```

**Ports**:
- Nginx: 80 → App: 8080
- PostgreSQL: внутренний сеть

### Обработка ошибок

**Валидация до начала работы**:
- Проверка GitHub token (API запрос)
- Проверка VPS доступности (SSH соединение)
- Проверка лимитов GitHub API
- Если любая проверка не прошла → ошибка с инструкциями

**Rollback не нужен**: валидация исключает сбои в процессе.

### Проверка лимитов GitHub API

- Запрос `GET /rate_limit` перед созданием репо
- Если превышены → ошибка с инструкциями (обновить план, подождать)
- Если недостаточно прав → предложение расширить scope токена

---

## Безопасность

### SSH ключи

**Отдельная пара на проект**:
- CLI генерирует `id_rsa_projectname` / `id_rsa_projectname.pub`
- Приватный → GitHub Secret `SSH_PRIVATE_KEY`
- Публичный → добавляется на VPS

**Почему безопасно**:
- Если один ключ скомпрометирован → страдает только один проект
- Нет единого ключа для всех проектов

### GitHub Secrets

**Только секреты в Secrets**:
- `VPS_HOST` — не секрет, но в Secrets для единообразия
- `VPS_USER` — не секрет, но в Secrets
- `SSH_PRIVATE_KEY` — критичный секрет

**Нет .env файлов** — всё в GitHub Secrets.

### Риски

1. **SSH ключи в GitHub Secrets** — стандартная практика, но требуются ограничения доступа к репо
2. **Автоматическая установка Docker на VPS** — требуется root доступ, потенциальная уязвимость
3. **GitHub token хранится локально** — если компьютер скомпрометирован, злоумышленник может создавать репо

**Митигация**:
- Использовать токены с ограниченным scope
- Хранить токен в зашифрованном виде (TODO)
- Ограничить доступ к VPS по IP (TODO)

---

## Тестирование

### Unit тесты с моками

**Что мокаем**:
- GitHub API (Ktor client mock)
- SSH подключения (SSH client mock)
- VPS команды (command executor mock)

**Примеры тестов**:
- Успешное создание проекта
- Ошибка авторизации GitHub
- VPS недоступен
- Лимиты GitHub API превышены

**Без интеграционных тестов** — только unit tests.

---

## Дистрибуция

### Standalone JAR

**Сборка**: Gradle Shadow Plugin
```bash
./gradlew shadowJar
# Результат: build/libs/project-starter-1.0.0-all.jar
```

**Установка**:
```bash
curl -O https://github.com/username/project-starter/releases/download/v1.0.0/project-starter.jar
chmod +x project-starter.jar
java -jar project-starter.jar create
```

**Алиас** (опционально):
```bash
alias project-starter='java -jar /path/to/project-starter.jar'
```

### Homebrew Tap (TODO)

Планируется, но не в MVP.

### Native binary (TODO)

GraalVM native image для быстрого старта — не в MVP.

---

## Документация

### README.md в CLI репо

**Содержание**:
- Назначение инструмента
- Установка (standalone JAR)
- Использование (пример диалога)
- Требования (JDK 17+, GitHub token, VPS с root доступом)
- Troubleshooting

### README.md в шаблоне проекта

**Содержание**:
- Стек проекта (Ktor + Exposed + PostgreSQL)
- Локальный запуск: `docker compose up`
- Деплой: автоматически через GitHub Actions
- Структура проекта
- Разработка: как добавить endpoint, миграцию БД

### --help

```bash
$ project-starter --help
Project Starter CLI — быстрый развёртывание Kotlin проектов

USAGE:
  project-starter create

OPTIONS:
  --help     Показать эту справку
  --version  Версия CLI
```

---

## Масштабируемость

### Фиксированный шаблон

**Только Ktor + Exposed + PostgreSQL + Nginx**

**Без плагинов/расширений** — это не инструмент для генерации любых проектов, а специально заточенный под Ktor MVP.

**Будущее** (не в MVP):
- Опциональные компоненты (`--no-db`, `--no-nginx`)
- Другие шаблоны (Spring Boot, Go)
- Плагинная система

---

## Future-proofing

### Без проверок версий

**Нет auto-update** — пользователи обновляют вручную:
```bash
curl -O https://github.com/.../project-starter.jar
```

**Проверка версий не нужна** — это увеличивает сложность, а польза минимальна для CLI.

### Возможные улучшения (TODO)

1. **Auto-update** — проверка версий при старте
2. **Шифрование токена** — хранение GitHub token в зашифрованном виде
3. **Homebrew Tap** — удобная установка
4. **Native binary** — GraalVM для быстрого старта
5. **Мульти-окружения** — dev/staging/prod VPS
6. **Rollback при ошибках** — удаление репо если VPS не настроен
7. **Шаблоны из внешнего репо** — лёгкое обновление шаблонов

---

## Критерии успеха

### Для челленджа

✅ **Есть пайплайн** — `.github/workflows/deploy.yml` в шаблоне
✅ **Пайплайн может не работать из-за ключей** — GitHub Secrets создаются CLI, но пользователь может не иметь VPS
✅ **Реальная задача** — прототипирование пет-проектов
✅ **Приложение решает задачу** — автоматизирует рутину (создание репо, настройка VPS, CI/CD)

### Для пользователя

✅ **Быстро** — от идеи до деплоя за 5 минут
✅ **Просто** — интерактивные вопросы, без флагов и конфигов
✅ **Работает** — Ktor stack с Nginx, production-ready
✅ **Безопасно** — отдельные SSH ключи, валидация до начала

---

## Стек технологий

### CLI

- **Язык**: Kotlin 1.9+
- **CLI фреймворк**: Clikt
- **GitHub API**: Ktor client
- **SSH**: Apache MINA SSHD / JSch
- **Шаблоны**: Встроенные ресурсы + placeholder-замены
- **Build**: Gradle (Kotlin DSL) + Shadow Plugin

### Шаблон проекта

- **Backend**: Ktor 2.x + Exposed + Kotlinx-html
- **Database**: PostgreSQL 15
- **Proxy**: Nginx latest
- **ORM**: Exposed (SQL DSL)
- **Build**: Gradle (Kotlin DSL)
- **CI/CD**: GitHub Actions (Docker build + SSH deploy)

---

## Dependencies

### CLI (build.gradle.kts)

```kotlin
dependencies {
    implementation("com.github.ajalt.clikt:clikt:4.2.0")
    implementation("io.ktor:ktor-client-core:2.3.4")
    implementation("io.ktor:ktor-client-cio:2.3.4")
    implementation("org.apache.sshd:sshd-core:2.11.0")
    implementation("org.apache.sshd:sshd-scp:2.11.0")
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
}
```

### Шаблон проекта (build.gradle.kts)

```kotlin
plugins {
    kotlin("jvm") version "1.9.20"
    application
}

application {
    mainClass.set("io.ktor.server.netty.EngineMain")
}

dependencies {
    implementation("io.ktor:ktor-server-core:2.3.4")
    implementation("io.ktor:ktor-server-netty:2.3.4")
    implementation("io.ktor:ktor-server-html-builder:2.3.4")
    implementation("org.jetbrains.exposed:exposed-core:0.44.1")
    implementation("org.jetbrains.exposed:exposed-dao:0.44.1")
    implementation("org.jetbrains.exposed:exposed-jdbc:0.44.1")
    implementation("org.postgresql:postgresql:42.6.0")
}
```

---

## Конфигурация CLI

### Файл конфигурации (опционально)

`~/.config/project-starter/config.toml`:

```toml
[github]
token = "sk-or-v1-..."  # Сохраняется после первого запуска

[vps]
default_host = "192.168.1.100"  # Опционально
default_user = "root"           # Опционально
```

**Без конфига** — всё через вопросы.

---

## Диаграмма пайплайна

```
┌─────────────────┐
│  Запуск CLI     │
│  project-starter│
│     create      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Интерактивные   │
│ вопросы:        │
│ - Название      │
│ - Описание      │
│ - VPS хост      │
│ - GitHub token  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Валидация:      │
│ - GitHub API    │
│ - VPS доступ    │
│ - Лимиты API    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Создание GitHub │
│ репозитория     │
│ через API       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Генерация       │
│ проекта из      │
│ шаблона         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Настройка GitHub│
│ Secrets:        │
│ - VPS_HOST      │
│ - VPS_USER      │
│ - SSH_PRIVATE_KEY│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Настройка VPS:  │
│ - Установка     │
│   Docker        │
│ - Генерация SSH │
│   ключей        │
│ - Добавление    │
│   публичного    │
│   ключа         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Git push в      │
│ GitHub          │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ GitHub Actions  │
│ автоматически:  │
│ - Build Docker  │
│ - Deploy на VPS │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ✅ Готово!      │
│ Проект доступен │
│ на VPS          │
└─────────────────┘
```

---

## Пример сгенерированного проекта

### src/main/kotlin/Application.kt

```kotlin
package io.github.username.myawesomeapp

import io.ktor.server.application.*
import io.ktor.server.engine.*
import io.ktor.server.netty.*
import io.ktor.server.response.*
import io.ktor.server.routing.*

fun main() {
    embeddedServer(Netty, port = 8080) {
        routing {
            get("/") {
                call.respondText("Hello from My Awesome App!")
            }
        }
    }.start(wait = true)
}
```

### docker-compose.yml

```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=jdbc:postgresql://db:5432/myapp
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=myapp
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
```

### .github/workflows/deploy.yml

```yaml
name: Deploy to VPS

on:
  push:
    branches: ['**']

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t myapp .

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker compose down
            docker compose pull
            docker compose up -d
```

---

## Заключение

Эта спецификация описывает **MVP инструмента** для автоматического развёртывания Kotlin пет-проектов.

**Ключевые решения**:
- ✅ Фиксированный стек (Ktor + Exposed + PostgreSQL + Nginx)
- ✅ Интерактивный UX без флагов
- ✅ Валидация до начала работы
- ✅ Простой SSH-деплой через GitHub Actions
- ✅ Отдельные SSH ключи на проект
- ✅ Standalone JAR дистрибуция
- ✅ Unit тесты с моками

**Результат для челленджа**: Есть пайплайн (`.github/workflows/deploy.yml`), который может не работать из-за ключей — GitHub Secrets создаются CLI, но пользователь должен предоставить VPS.
