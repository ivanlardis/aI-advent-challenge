# 🚀 Project Starter CLI - Инструкция по настройке

## ⚙️ Настройка перед первым использованием

### 1. Создайте GitHub Personal Access Token

Перейдите в https://github.com/settings/tokens и нажмите "Generate new token" → "Generate new token (classic)"

**ВАЖНО! Выберите следующие scopes:**
- ✅ `repo` - full control of private repositories
- ✅ `workflow` - для управления GitHub Actions файлами (ОБЯЗАТЕЛЬНО!)

Скопируйте токен (показывается только один раз!).

### 2. Создайте файл .env

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```bash
# GitHub Token (создали на шаге 1)
GITHUB_TOKEN=ghp_ваш_токен_здесь

# VPS Configuration
VPS_HOST=213.171.25.139
VPS_USER=ivanlardis

# VPS Password
VPS_PASSWORD=ваш_пароль_здесь
```

### 3. Настройте SSH ключи на VPS (рекомендуется)

Вместо пароля лучше использовать SSH ключи:

```bash
ssh-copy-id ivanlardis@213.171.25.139
```

Или вручную:

```bash
cat ~/.ssh/id_ed25519.pub | ssh ivanlardis@213.171.25.139 'mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys'
```

### 4. Соберите CLI

```bash
./gradlew clean build shadowJar
```

## 📦 Использование

### Создание нового проекта

Одной командой:

```bash
java -jar build/libs/project-starter-1.0.0-all.jar create -n "my-awesome-project" -d "My awesome Kotlin project"
```

**Что произойдёт автоматически:**

1. ✅ Создастся GitHub репозиторий `my-awesome-project`
2. ✅ Сгенерируется Kotlin проект (Ktor + PostgreSQL + Docker Compose)
3. ✅ Настроятся GitHub Secrets (VPS_HOST, VPS_USER, SSH_PRIVATE_KEY)
4. ✅ Код автоматически отправится в GitHub
5. ✅ GitHub Actions задеплоит проект на VPS
6. ✅ Через 2-3 минуты проект будет доступен на http://213.171.25.139

### Демо режим (для тестирования без реальных действий)

```bash
java -jar build/libs/project-starter-1.0.0-all.jar create -n "test" -d "Test" --demo
```

## 🔧 Проверка результата

### 1. Откройте GitHub репозиторий

```
https://github.com/ivanlardis/my-awesome-project
```

### 2. Проверьте GitHub Secrets

Settings → Secrets → Actions → должны быть:
- `VPS_HOST`
- `VPS_USER`
- `SSH_PRIVATE_KEY`

### 3. Проверьте GitHub Actions

Actions → последняя runnable workflow должна показать зелёный статус ✅

### 4. Откройте сайт

```
http://213.171.25.139
```

Должна появиться страница "Welcome to my-awesome-project!"

## 🔄 Автоматический деплой при каждом изменении

Теперь каждый `git push` в репозиторий автоматически:

1. Соберёт Docker образ
2. Задеплоит на VPS
3. Перезапустит контейнеры

## ❓ Troubleshooting

### Ошибка "Bad credentials"

- Убедитесь что токен действительный
- Проверьте что в токене есть scopes: `repo` И `workflow`

### GitHub Actions не запускается

- Проверьте что в репозитории есть файл `.github/workflows/deploy.yml`
- Проверьте Secrets в настройках репозитория

### Проект не доступен на VPS

- Проверьте что Docker установлен на VPS: `docker --version`
- Проверьте логи GitHub Actions
- Проверьте контейнеры на VPS: `ssh vps "docker ps"`
